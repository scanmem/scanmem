TODOs
=====

- plugin-like system to support different datatypes (or actually representations)
- data check (if a valid number & if may cause overflow)
- syntax check 
- error detection and alert
- run backend in a separate thread
- search for pointer
- text
- different types of locking ('>' & '<')
- syntax support 
  * 4,5 -- (values between 4 and 5)
        -- might be difficult if signedness involved
- memory view/edit
- UI improvement will be done only when needed!
